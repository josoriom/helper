// import 'dart:math';

// void calcCoefficients(List<Map<String, double>> tuple, List<double> powers) {
//   List<List<double>> X = [];
//   List<List<double>> Y = [];
//   for (int i = 0; i < tuple.length; i++) {
//     Y[i] = [tuple[i]['y']!];
//     X[i] = List.generate(powers.length, (index) => 0.0);
//     for (int j = 0; j < powers.length; j++) {
//       X[i][j] = pow(tuple[i]['x']!, powers[j]).toDouble();
//     }
//   }
// }
